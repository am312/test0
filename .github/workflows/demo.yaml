name: demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: 
  release:
    types: [published]
env:
  VERSION: ${{ github.event.release.name }}
  REF: ${{ github.ref_name }}
jobs:
  job-one:
    runs-on: ubuntu-latest
    steps:
      - run: echo "0 ${{ github.event.release.tag_name }}"
  job-two:
    runs-on: ubuntu-latest
    steps:
      - run: echo "1 ${{ github.event.release.tag_name }}"



